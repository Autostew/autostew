language: python
python:
  - "3.5"
install:
  - pip install -r requirements.txt
  - pip install coveralls
script:
  - ./recreate_db.sh
  - coverage run --branch --source . manage.py test --settings=autostew.settings.dev
after_success: coveralls
